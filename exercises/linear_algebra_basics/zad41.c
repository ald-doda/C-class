#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(){
    double O;
    printf("Unesite obim kruga ciju povrsinu i poluprecnik zelite izracunati:\n");
    scanf("%lf", &O);

    #INCLUDE PI 3.14;
    double r = O/(2*PI)
    double pow(r, 2) * PI;
    printf("Povrsina kruga za dati obim je je %lf, a poluprecnik kruga za obim O = %lf je %lf.\n", P, O, r);
    return 0;
}